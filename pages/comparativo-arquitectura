import streamlit as st

st.set_page_config(page_title="Comparativa de Soluciones", layout="wide")
st.title("Comparativa de Soluciones y Argumento Técnico")

st.markdown("""
| Alternativa | Pros | Contras | Relevancia para el caso |
|-------------|------|---------|-------------------------|
| **Local + MongoDB** | Fácil de implementar, sin gastos adicionales | Escalabilidad baja | ✅ Ideal para pruebas educativas |
| **AWS (S3 + RDS)** | Alta confiabilidad, respaldo automático | Necesita cuenta y pago | ⚠️ Demasiado complejo para prototipo |
| **Serverless (Glue + Athena)** | Sin administración de infraestructura, pago por uso | Requiere aprendizaje extra | ❌ No apropiado para este alcance |
""")

st.info("""
**Decisión**: _Local + MongoDB_ satisface los requisitos de bajo costo, facilidad de uso y
permite una evaluación sencilla por parte del equipo docente.
""")

try:
    st.image("static/arquitectura.png", caption="Esquema de la solución técnica", use_container_width=True)
except:
    st.warning("No se encontró el archivo arquitectura.png en la carpeta static/")
